# Archived Scripts

These scripts are no longer part of the active pipeline but are kept for reference.

| Script | Reason |
|---|---|
| `discover_universe.py` | Replaced by `fetch_cb_universe.py` (data_pool one-call) |
| `generate_themes_with_claude.py` | Replaced by `generate_themes_direct.py` (local keyword rules) |
| `load_themes.py` | Themes now loaded via `generate_themes_direct.py` directly |
| `sample_one.py` | One-off smoke test, no longer needed |
| `build_strategy_page.py` | Strategy integrated into main dashboard (5A decision) |
